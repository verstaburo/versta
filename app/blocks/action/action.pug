mixin action(title, text, button)
  +b.action&attributes(attributes)
    +e.col
      +e.P.title!= title
      +e.P.text!= text
    +e.col
      +button(false, true)(href="#start")._filled.__button.js-fancybox!= button
